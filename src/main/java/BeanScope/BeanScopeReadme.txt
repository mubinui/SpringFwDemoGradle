Bean Scope
    -> Singleton
        Creates single object and give the reference of the same object every time . Scope by default Singleton
        All hash codes are same as they all are the hash code of athe same object

    -> Prototype
        Everytime creates new object by the spring container .+
        Hash codes are different every time .

    -> Request
        Works at the time of working with web app ie http request

    -> Session
        Web app

    -> Global Session
